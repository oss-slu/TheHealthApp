# Applied automatically for `vite build` (and overridable with `.env.production.local`, not committed).
# Demo/offline shortcuts must stay disabled in production so auth and APIs always use real backends.
VITE_DEMO_MODE=0
